import { ServerResolver } from "../libs/server-resolver.js";

// const serverResolver = new ServerResolver(["http://localhost:3001", "http://localhost:3002", "http://localhost:3003"]);
const serverResolver = new ServerResolver();

export default serverResolver;
